# 待解决问题清单

## 紧急问题

### 1. Token验证部分失效
**症状**: 部分API返回"无法验证凭据"

**测试通过的API**:
- ✓ `/api/merchants/login` - 登录
- ✓ `/api/merchants/info` - 商家信息
- ✓ `/api/config/shop` - 店铺配置
- ✓ `/api/config/group-buys` - 团购配置
- ✓ `/api/data/active` - 活跃订单
- ✓ `/api/history` - 历史记录
- ✓ `/api/merchants/stats` - 商家统计

**可能需要测试的API**:
- ? `/api/data/active` (POST) - 更新活跃数据
- ? `/api/merchants/users` - 用户管理
- ? `/api/config/group-buys` (POST/PUT/DELETE) - 团购管理

**排查方向**:
1. 检查请求头格式是否正确
2. 确认token是否在有效期内
3. 检查依赖注入是否正确

---

## 待实现功能

### 2. 订单操作API（从main.py迁移）

| 原API | 新路径 | 优先级 | 工作量 |
|--------|--------|--------|--------|
| POST /api/tables/open | POST /api/tables/open | ⭐⭐⭐⭐⭐ | 大 |
| POST /api/tables/bill | POST /api/tables/bill | ⭐⭐⭐⭐⭐ | 大 |
| POST /api/tables/checkout | POST /api/tables/checkout | ⭐⭐⭐⭐⭐ | 大 |
| DELETE /api/tables/{order_id} | DELETE /api/tables/{order_id} | ⭐⭐⭐⭐ | 中 |
| POST /api/tables/pause | POST /api/tables/pause | ⭐⭐⭐⭐ | 大 |
| POST /api/tables/suspend | POST /api/tables/suspend | ⭐⭐⭐ | 中 |
| POST /api/tables/cancel-suspend | POST /api/tables/cancel-suspend | ⭐⭐⭐ | 中 |
| POST /api/tables/add-time | POST /api/tables/add-time | ⭐⭐⭐ | 中 |
| POST /api/tables/remark | POST /api/tables/remark | ⭐⭐ | 小 |
| POST /api/tables/verify | POST /api/tables/verify | ⭐⭐⭐ | 中 |

### 3. 数据迁移工具
**文件**: `init_db.py` 中的 `migrate_from_files()` 函数

**需要完成**:
- [ ] 解析 `active_data.json` 并转换到 orders 表
- [ ] 解析 `history_data.json` 并转换到 order_history 表
- [ ] 指定目标商家ID (shop_id)
- [ ] 关联暂停日志、加时记录等

### 4. 短信验证码集成
**当前**: 验证码输出到控制台

**需要**:
- [ ] 集成阿里云/腾讯云短信服务
- [ ] 配置API密钥
- [ ] 添加发送频率限制

---

## 优化建议

### 5. 性能优化
- [ ] 添加数据库索引
- [ ] 实现查询缓存
- [ ] 优化N+1查询

### 6. 安全加固
- [ ] 从环境变量读取敏感配置
- [ ] 添加API速率限制
- [ ] 实现完整的操作审计日志

### 7. 用户体验
- [ ] 完善错误提示信息
- [ ] 添加前端表单验证
- [ ] 实现完整的收银台界面

---

## 开发建议

### 第一步：验证现有API完整性
```bash
cd web_app
python test_saas.py
```

### 第二步：修复Token验证问题
- 检查所有使用 `@app.get` 等装饰器的API
- 确保都正确使用了认证依赖

### 第三步：迁移订单操作
- 从 `main.py` 复制相关函数
- 添加 `shop_id` 过滤
- 添加认证依赖

### 第四步：测试完整流程
- 注册新商家
- 登录
- 开台
- 结账
- 查看历史

---

**最后更新**: 2026年4月17日
